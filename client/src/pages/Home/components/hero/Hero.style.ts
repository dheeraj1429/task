import styled from 'styled-components';

export const HeroContainer = styled.section`
   padding: 5rem 0;

   .hero-heading {
      margin: 0;
      font-size: 5.3rem;

      .sp-heading {
         color: var(--light-blue);
      }
   }

   .hero-para {
      margin-top: 1rem;
      font-size: 0.9rem;
      color: var(--gray-cl);
   }
`;
