import styled from 'styled-components';

export const ResponserContainer = styled.section`
   width: 100%;
   padding: 1rem 5rem;

   .para {
      text-align: center;
      font-size: 1rem;
      margin-bottom: 1rem;
      color: var(--gray-cl);
   }
`;
